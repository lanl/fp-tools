Configuration File and Parameters
=================================

All simulations are configured using a single `TOML <https://toml.io/en/>`__ 
configuration file. This file specifies the position and velocity files, 
analysis options, and output preferences.

Overview
--------

The configuration file is structured into sections that correspond to different 
parts of the toolkit:

- `[input]`: Path to the trajectory file and optional metadata
